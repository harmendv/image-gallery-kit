# Playground

Use this page to test every `ImageGallery` prop against the real package import and a shared demo image set.

The controls above the preview use docs-local `shadcn-vue` components with isolated styling so they keep their intended appearance inside VitePress.

<div class="vp-raw">
  <GalleryPlayground />
</div>
