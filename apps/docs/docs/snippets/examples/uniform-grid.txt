<ImageGallery :images="images">
  <div class="grid grid-cols-3 gap-3">
    <ImageGalleryImage
      v-for="image in images.slice(0, 6)"
      :key="image.id"
      :image="image"
      class="aspect-square rounded-xl"
    />
  </div>

  <!-- One `aspect-square` on the tile beats each image's own ratio; `object-cover` crops to fill. -->
  <template #dialog>
    <ImageGalleryOverlay>
      <template #topbar>
        <ImageGalleryTopbar>
          <template #start><ImageGalleryGridToggle /></template>
          <template #center><ImageGalleryCounter /></template>
          <template #end><ImageGalleryCloseButton /></template>
        </ImageGalleryTopbar>
      </template>

      <ImageGalleryStage />

      <!-- `uniform` tells the packing the tiles share one shape, so the columns stay even. -->
      <ImageGalleryGrid uniform class="grid-cols-3 gap-2 md:grid-cols-5">
        <template #tile="{ image, index }">
          <ImageGalleryGridTile :image="image" :index="index" class="aspect-square rounded-lg">
            <template #image><ImageGalleryGridImage class="object-cover" /></template>
          </ImageGalleryGridTile>
        </template>
      </ImageGalleryGrid>
    </ImageGalleryOverlay>
  </template>
</ImageGallery>
