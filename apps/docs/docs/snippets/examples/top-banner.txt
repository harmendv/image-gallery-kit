<ImageGallery :images="images">
  <div class="flex flex-col gap-3">
    <ImageGalleryImage :image="images[0]" class="aspect-[21/9] rounded-2xl" />

    <div class="grid grid-cols-3 gap-3">
      <ImageGalleryImage
        v-for="image in images.slice(1, 4)"
        :key="image.id"
        :image="image"
        class="aspect-square rounded-xl"
      />
    </div>
  </div>

  <!-- The caption slot sits inside the stage's body, under the stack. -->
  <template #dialog>
    <ImageGalleryOverlay>
      <template #topbar>
        <ImageGalleryTopbar>
          <template #end><ImageGalleryCloseButton /></template>
        </ImageGalleryTopbar>
      </template>

      <ImageGalleryStage>
        <template #caption="{ image, index, total }">
          <p class="text-sm font-medium">{{ image.alt }}</p>
          <p class="text-xs opacity-60">{{ index + 1 }} / {{ total }}</p>
        </template>
      </ImageGalleryStage>

      <ImageGalleryGrid />
    </ImageGalleryOverlay>
  </template>
</ImageGallery>
