<!-- `flex-col-reverse` puts the banner underneath without reordering the markup. -->
<ImageGallery :images="images">
  <div class="flex flex-col-reverse gap-3">
    <ImageGalleryImage :image="images[0]" class="h-48 rounded-2xl" />

    <div class="grid grid-cols-3 gap-3">
      <ImageGalleryImage
        v-for="image in images.slice(1, 4)"
        :key="image.id"
        :image="image"
        class="aspect-square rounded-xl"
      />
    </div>
  </div>

  <!-- The bar moves to match: a class on it, and the stage still clears it. -->
  <template #dialog>
    <ImageGalleryOverlay>
      <template #topbar>
        <ImageGalleryTopbar class="inset-x-0 bottom-0 top-auto border-b-0 border-t">
          <template #start><ImageGalleryGridToggle /></template>
          <template #center><ImageGalleryCounter /></template>
          <template #end><ImageGalleryCloseButton /></template>
        </ImageGalleryTopbar>
      </template>

      <ImageGalleryStage />
      <ImageGalleryGrid class="pt-4" />
    </ImageGalleryOverlay>
  </template>
</ImageGallery>
